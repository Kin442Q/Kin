# Скопируйте в .env (если используете) и при необходимости поменяйте.
# Vite требует префикс VITE_ для переменных, которые попадают в клиент.
VITE_API_URL=http://localhost:4000/api
VITE_SOCKET_URL=http://localhost:4000
